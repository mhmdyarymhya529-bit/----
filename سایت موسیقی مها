<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>سایت موسیقی ساده - ۶۶ ترک</title>
  <style>
    body {
      margin: 0;
      background: #000;
      color: #f44336; /* قرمز */
      font-family: Tahoma, sans-serif;
    }
    header {
      padding: 16px;
      border-bottom: 1px solid #f44336;
      text-align: center;
    }
    .list {
      padding: 16px;
      display: grid;
      grid-template-columns: 1fr;
      gap: 12px;
      max-width: 800px;
      margin: 0 auto;
    }
    .item {
      padding: 10px;
      border: 1px solid #f44336;
      border-radius: 6px;
      background: #111;
    }
    .label {
      margin-bottom: 8px;
      font-weight: bold;
    }
    audio {
      width: 100%;
      outline: none;
      filter: hue-rotate(330deg) saturate(1.2);
    }
    footer {
      border-top: 1px solid #f44336;
      padding: 12px 16px;
      text-align: center;
      font-size: 12px;
      color: #ff6b6b;
    }
  </style>
</head>
<body>
  <header>
    <h1>سایت موسیقی ساده</h1>
    <div>۶۶ ترک با پس‌زمینه سیاه و قرمز</div>
  </header>

  <main class="list" id="list"></main>

  <footer>فایل‌های صوتی باید کنار همین فایل با نام‌های music1.mp3 تا music66.mp3 باشند.</footer>

  <script>
    // تعداد ترک‌ها
    const TOTAL = 66;
    const list = document.getElementById('list');

    // تولید آیتم‌ها
    for (let i = 1; i <= TOTAL; i++) {
      const item = document.createElement('div');
      item.className = 'item';

      const label = document.createElement('div');
      label.className = 'label';
      label.textContent = `ترک ${i}`;

      const audio = document.createElement('audio');
      audio.controls = true;
      audio.src = `./music${i}.mp3`; // فرض: فایل‌ها کنار index.html

      item.appendChild(label);
      item.appendChild(audio);
      list.appendChild(item);
    }
  </script>
</body>
</html>
